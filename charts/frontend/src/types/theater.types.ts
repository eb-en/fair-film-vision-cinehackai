export interface Theater {
  id: string;
  name: string;
  region: string;
  distributorId: string;
}

export interface TheaterSearchResult {
  id: string;
  title: string;
  region: string;
  slug: string;
  groupTitle: string;
}
